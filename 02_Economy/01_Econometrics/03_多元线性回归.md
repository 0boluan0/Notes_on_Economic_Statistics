 
# 多元线性回归(Multiple Linear Regression)的两个例子

## CD生产函数(Cobb-Douglas Production Function)

即柯布道格拉斯生产函数(Cobb-Douglas Production Function)
$$ \oint_{I}\equiv{\cal A}\!N^{\oint_{I}}L_{i}^{\beta_{2}}e^{\varepsilon_{i}} $$
![[Pasted image 20240316170040.png]]
虽然在这种情况下不是线性的，但是可以对两边取对数来得到线性形式：
$$
\ln Q_i=\beta_0+\beta_1\ln K_i+\beta_2\ln L_i+\varepsilon_i,\qquad
\varepsilon_i\sim N(0,\sigma^2).
$$

## 新凯恩斯混合菲利普斯曲线

$$
\pi_t=\gamma_b\pi_{t-1}+\gamma_fE_t\pi_{t+1}+\gamma_{mc}mc_t
$$

## 多元线性回归的一般形式

$$
Y_i=\beta_0+\beta_1X_{1i}+\cdots+\beta_KX_{Ki}+\varepsilon_i,\qquad i=1,2,\ldots,N.
$$
## 偏效应

在数学上相当于偏导，具有很好的经济学意义，反映一个解释变量改变且其他解释变量不变时 $Y$ 的边际影响。~~但是事实上是不存在的~~（现实中“其他不变”难以严格成立，偏效应是一种局部分析假设）。

# 多元线性回归模型的OLS估计

## 回归系数的估计

求离差平方和最小，得OLS估计量。
即求偏导得0
i=l] i=] 四 N a ~ ~ nw =min 》(7 — By ~ BX — 1 — BEX VE; i=l 根据 其 一 阶 优化 条 件 : Oy 2 ap
一阶条件可写为：对每个参数求偏导并令其为 0。
得正规方程组
N 和 2 & = 0 an Xe; =0 . : 2 X KE; =0
![[Pasted image 20240329171704.png]]
也就是对每个解释变量都有正交条件：$\sum_{i=1}^n x_{ji}\hat e_i=0,\ j=0,1,\dots,k$。

正规方程组很难算,将其变换为行列式的形式.
$$ \begin{array}{c c c c c c c}{{\left[\begin{array}{c c c c c}{{1}}&{{1}}&{{\cdots}}&{{1}}\\ {{\frac{1}{x_{11}}}}&{{x_{12}}}&{{\cdots}}&{{x_{1n}}}\end{array}\right]\left[y_{1}\right]\left[\begin{array}{c c c c c}{{1}}&{{x_{11}}}&{{\cdots}}&{{1}}\\ {{\left[x_{11}}}&{{x_{12}}}&{{\cdots}}&{{x_{k n}}}\end{array}\right]\left[\frac{1}{1}}&{{x_{12}}}&{{\cdots}}&{{x_{k1}}\right]\left[\frac{1}{\beta}_{0}\right]}}\\ {{\left[x_{k1}}}&{{x_{k2}}}&{{\cdots}}&{{x_{k n}}}&{{x_{k n}}}\\ {{\left.\right.}}&{{\left.\cdots}}&{{\left[\begin{array}\frac{\beta}}}&{{\delta}}&{{x_{\right]}}}&{{\left[\right]\left[\right]}}\\ {{\end{array} $$
![[Pasted image 20240606122411.png]]
得到$\widehat{\beta}=(X^{\prime}X)^{-1}X^{\prime}Y.$![[Pasted image 20240606123443.png]]

### 多重共线性

回归模型的解释变量之间不能存在完全的多重共线性。多重共线性是指在多元线性回归模型中，自变量之间存在高度相关性的情况。这种情况会导致回归系数的估计不准确，使得模型的结果难以解释和预测。简单来说，就是自变量之间有很强的相关性，导致模型难以准确地估计出每个自变量对因变量的影响。

完全的多重共线性：是指一个解释变量是、其他解释变量的线性组合 。 说明该解释变量所提供的信息与其他解释变量是完全重复的。

### 多元线性回归的高斯马尔可夫定律

仅在一元线性回归的基础上增加了一条。即不能存在完全的多重共线性。

### 系数计算：以二元回归模型为例

以二元回归为例，模型可写为
$$
Y_i=\beta_0+\beta_1X_{1i}+\beta_2X_{2i}+\varepsilon_i.
$$
最小化残差平方和可得到正规方程组（正交条件）：
$$
\sum_{i=1}^n \hat\varepsilon_i=0,\qquad
\sum_{i=1}^n X_{1i}\hat\varepsilon_i=0,\qquad
\sum_{i=1}^n X_{2i}\hat\varepsilon_i=0.
$$
由 正规 方程 组 求解 ， 得 到 回归 系数 的 估计 量 : B _ yO X53) — Ol VO HX) B _ yO) - Oly OL HX) B =Y-£X, — B,X,
![[Pasted image 20240331150638.png]]
基于 方差 公式 得 到 各 回归 系数 估计 量 的 方差: var(B,) = Sa ee = Ire cov( fs) = EN os
![[Pasted image 20240331150722.png]]

## 随机误差项方差的估计

$$ \begin{array}{c}{{\mathrm{var}(\varepsilon_{i}|X_{1i},\cdots,X_{k i})=\sigma^{2}~\mathrm{fij}\overline{{{\mathcal D}}}\mathrm{fii}^{\pm}\mathrm{i}^{\pm}\mathrm{i}^{\pm}\mathrm{j}\mathrm{f}\sqrt{\frac{\mathrm{i}}{\mathrm{i}}\mathrm{i}^{\pm}\mathrm{i}^{\pm}\mathrm{j}\sqrt{\frac{\mathrm{i}}{\mathrm{j}}\sqrt{\mathrm{j}}\mathrm{j}\mathrm{j}\mathrm{j}\mathrm{f}\mathrm{f}\mathrm{i}}\bigg/\mathrm{f}\mathrm{f}\bigg/{\frac{\mathrm{i}}{\mathrm{j}}{\mathrm{j}}\bigg/{\mathrm{f}}\bigg/}\mathrm{f}\bigg/{\frac{\mathrm{i}}{\mathrm{j}}\bigg/{\mathrm{f}}\bigg/{\mathrm{f}}\bigg/{\frac{\mathrm{i}}{\mathrm{f}}\bigg/{\mathrm{f}}\bigg/{\mathrm{f}}}\bigg/{\mathrm{f}}\mathrm{f}}}}\\ {{{{{\hat{\mathrm{\mathrm{\cal{\cal{\cal{f}}}}}}}\mathrm{\bigg)}\quad}}\quad}}}\end{array} $$
![[Pasted image 20240331160623.png]]

自由度是 $N-K-1$，因为总共有 $N$ 个~~不等式~~ 等式（样本观测），估计了 $K+1$ 个参数（正规方程组约束）。

O 小 结 表 3-2 OLSE 的 统计 性 质 与 假定 有 限 样本 大 样本 假定 MLR.1: 参数 线性 假定 MLR.1: 参数 线性 假定 MLR.2 随 机 抽样 假定 MLR.2: 随机 抽样 |) 假定 MLR.3: 无 完全 多 重 共 线 性 从 正 | 假定 MLR.3: 无 完全 多 重 共 线 性 BK | ut | 进 假定 MLR.4: 零 条 件 均 值 〈 解 释 变 量 性 性 态 假定 MLR.4':， 零 均值 和 正 交 性 〈 解 释 性 有 | 正 iid 性 | 变量 外 生 ) 效 | 态 假定 MLR.5: 条 件 同方 差 假定 MLR.S， 条 件 同方 差 和 | 假定 MLR.6:， 误差 项 正 态 分 布 Pf |
可按有限样本/大样本两类总结 OLS 性质：在外生性与无完全共线性下无偏且一致；附加同方差与正态性时可做经典精确推断。

## 判定系数的调整

原先的 TSS、RSS、ESS 反映拟合程度，但未考虑参数个数。若无限增加解释变量，$R^2$ 会单调上升、可无限逼近 1。为避免过拟合，需要引入调整后的 $\bar R^2$。
$$ \overline{{{R}}}^{\,2}=1\!-\!\frac{R S S/(N\!-\!K\!-\!K\!-\!1)}{T\!S S/(N\!-\!1)} $$

调整后如果想要增加新的解释变量的话就要考虑自由度和解释能力的权衡了。

# 多元线性回归模型的假设检验

## 单参数检验:T检验

使用t检验
只能检验单个参数；~~分别检验不同的参数是无意义的~~ 更严谨做法是结合联合 F 检验或调整多重检验带来的显著性偏差。
和一元线性回归模型只有自由度的区别，别的是一样的。

此时$\begin{array}{l}{{\mathrm{~if~}}}\\ {{\mathrm{~in}\bigl(\widetilde{\mathrm{j}}\bigl)\widetilde{\mathrm{j}}\biggl(\widetilde{\mathrm{j}}\biggl(\widetilde{\mathrm{j}})\widetilde{\mathrm{j}}\biggl(\widetilde{\mathrm{j}}\epsilon\widetilde{\mathrm{j}}\epsilon\widetilde{\mathrm{j}}\epsilon\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\biggl(\mathrm{j}_{\mathrm{j}}\epsilon\widetilde{\mathrm{j}}\epsilon\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}\widetilde{\mathrm{j}}}}}}\\ {{{\left(\mathrm{\mathrm{\bfj}}\widetilde{\mathrm{j}\widetilde{\widetilde{\bf\bf f}\overline{\overline{j}}\widetilde{\widetilde{\mathrm{\mathrm{j}}}}\widetilde{\widetilde{\widetilde{\widetilde{\epsilon}}}}\widetilde{\widetilde{\widetilde{\widetilde{\widetilde{\widetilde{\mathrm{\widetilde{\widetilde{\widetilde{\epsilon}}}}\widetilde{\widetilde{\widetilde{\widetilde{\widetilde{j}}\widetilde{\widetilde{\widetilde{\widetilde{\widetilde{\widetilde{\widetilde{$![[Pasted image 20240610154140.png]]

其中(1/1-R1方)是方差膨胀因子,是这个解释变量对别的解释变量求回归的方程的R方.R方越大说明独有信息越小,此时方差越大

$$ \begin{array}{c}{{(\,4\,)\mathrm{~}\,\hat{\mathrm{K}}\hat{\Sigma}\hat{\cal{B}}^{\prime}}\cdot(\hat{\beta}_{1}+\hat{\beta}_{2})-(\beta_{1}+\beta_{2})\sim N(0,1)}}\\ {{\mathrm{}}}\\ {{\mathrm{}}}\\ {{\mathrm{}}}\\ {{\mathrm{}}}\\ {{\mathrm{}}}\\ {{\mathrm{}\frac{(\hat{\beta}_{1}+\hat{\beta}_{2})-(\beta_{1}+\hat{\beta}_{2})}\sim t(N-K-1)}}\end{array} $$
![[Pasted image 20240331200913.png]]

## 多参数检验：F 检验

验证多个参数是否同时成立。
m 无 约束 回归 方程 InQ, = B, 十 局 Ink, + B, In L, + é, 将 其 RSS 记 为 RSS,. ， 自 由 度 为 N-3. a 将 原 假设 中 的 约束 条 件 带 入 回归 方程 ， 得 到 了 所 谓 的 “有 约束 回归 方程 ” 。 Ind, = By + B, Ink, +(1—£,)In L, +é, 将 其 RSS 记 为 RSys.， 自 由 度 为 N-2.
设无约束与有约束模型残差平方和分别为 $RSS_u, RSS_r$，据此构造多参数联合 F 检验。
AEF RSS, 和 RSS, ， 在 原 假 设 成 立 的 情况 下 ， 有 六 -CRSS = RSS Fa 3) RSS,,, (N —3) 如 果 原 假设 为 真 ， 我 们 会 倾向 于 得 到 较 小 的 F 值 。 反之 ， 我 们 会 倾向 于 得 到 较 大 的 F 值 。 判定 : 若 F 值 大 于 临界 值 ， 或 p 值 小 于 显著 性 水 平 ， 则 丰 绝 厦 人 假设。
![[Pasted image 20240331201029.png]]
检验 统计 量 ZEFRSS,, 和 RSS, ， 在 原 假设 成 立 的 情况 下 ， 有 -CRSS. RSF peg N_K_ RSS, (N-K —1) RRA, BAIS wT SB) AY F 反之 ， 我 们 会 倾向 于 得 到 较 大 的 F 值 。
常用统计量为 $F=\frac{(RSS_r-RSS_u)/q}{RSS_u/(n-k-1)}$，据临界值或 $p$ 值判定。


### F检验的基本原理

F检验通过比较模型的方差来判断模型是否显著。具体来说，它比较的是在加入解释变量前后的模型残差平方和的差异。

### F检验统计量

F检验统计量的计算公式如下：

$$
F = \frac{(RSS_{R} - RSS_{UR}) / (df_{R} - df_{UR})}{RSS_{UR} / df_{UR}}
$$

其中：

•	 $RSS_{R}$  是受限模型的残差平方和。
•	 $RSS_{UR}$  是非受限模型的残差平方和。
•	 $df_{R}$  是受限模型的自由度。
•	 $df_{UR}$  是非受限模型的自由度。

F统计量服从  F  分布，其自由度为 $(df_{R} - df_{UR}, df_{UR})$。

### F检验的步骤

1.	构建模型：
	•	受限模型：不包含某些解释变量的模型。
	•	非受限模型：包含所有解释变量的模型。
2.	计算残差平方和（RSS）：
	•	计算受限模型和非受限模型的残差平方和。
3.	计算F统计量：
	•	根据上述公式计算F统计量。
4.	比较临界值：
	•	将计算的F统计量与F分布的临界值进行比较，判断是否拒绝原假设。

### 具体步骤示例

假设我们有以下回归模型：

受限模型（不包含某些解释变量）：
$$
y_i = \beta_0 + \beta_1 x_{1i} + \epsilon_i
$$

非受限模型（包含所有解释变量）：
$$
y_i = \beta_0 + \beta_1 x_{1i} + \beta_2 x_{2i} + \beta_3 x_{3i} + \epsilon_i
$$

数学过程

1.	构建受限模型和非受限模型：

受限模型：
$$
y_i = \beta_0 + \beta_1 x_{1i} + \epsilon_i
$$

非受限模型：
$$
y_i = \beta_0 + \beta_1 x_{1i} + \beta_2 x_{2i} + \beta_3 x_{3i} + \epsilon_i
$$

2.	计算残差平方和（RSS）：

受限模型的残差平方和：
$$
RSS_{R} = \sum_{i=1}^{n} (y_i - \hat{y}_{R,i})^2
$$

非受限模型的残差平方和：
$$
RSS_{UR} = \sum_{i=1}^{n} (y_i - \hat{y}_{UR,i})^2
$$

3.	计算自由度（df）：

受限模型的自由度：
$$
df_{R} = n - k_R
$$
其中，$k_R$ 是受限模型中的参数个数。

非受限模型的自由度：
$$
df_{UR} = n - k_{UR}
$$
其中，$k_{UR}$ 是非受限模型中的参数个数。

4.	计算F统计量：

$$
F = \frac{(RSS_{R} - RSS_{UR}) / (df_{R} - df_{UR})}{RSS_{UR} / df_{UR}}
$$

F 检验通常为单侧拒绝型（右尾），不做双侧检验；它用于比较“受限 vs 非受限”模型，适合一次检验多个约束。~~看不来是小了还是大了~~

### F检验的一个例子:邹检验

检验92年前后中国宏观生产函数是否改变写俩方程,无约束的认为俩方程是分开的,,有约束的认为俩方程参数相等.无约束的RSS就是俩方程的简单加和,有约束的就是那个方程的rss.

4: 经 济 关 系 的 结构 稳定 性 检验 ， FE 检 验 的 一 待 检验 假设 ; 太 .w -wp a=8 个 例 了 种 检验 H ， me Ribchenica teat PR = il: 中 国 宏观 生产 函数 在 1992 年 前 后 是 否 不 同 ? 人 无 约束 回归 : 参数 可 以 不 同 F 检 验 统计 量 : 1978~19924%:; INO, =f, +f, nK,+f, nL, +é, pp = RSS, - RSS,,)/3_ __ (RSS, — RSS, ~ RSS,)/3 1993~2006: nd, =a,+a,mMK,+a,InLl, +6,, RSS, ((N, + N, — 6) (RSS, + RSS, )/(N, +N, — 6) RSS, = RSS, + RSS, RBA, HYMN F ~FG,N-6) 受 约束 回归 : 参数 不 变 本 例 中 ， 邹 检验 结果 :已 = 20.46625，P-0.0000。 1878 一 2006 下 INQ, = By +B InK, + Bab, + ey | 结论 : 拒绝 中 国生 产 函 数 保持 稳定 的 原 假设 。
邹检验本质上也是比较“分段回归 vs 合并回归”的残差平方和差异。

邹检验不好,因为样本容量不同.方差不一样,样本容量大了方差小.而且做邹检验要做三个回归.  

且不可识别差异的来源.

# 极大似然估计与似然比检验

## 极大似然估计

最大似然思想：在给定自变量的条件下，选择能使样本“最可能发生”的参数值；在常见正则条件下，大样本下一致/渐近正态。
实现：构造似然的乘积，取对数后对参数求导并令一阶条件为 0 求解。~~所以吧所有的出现的x对应的y的概率乘起来取对数求偏导令导得零就能得到估计值.~~

## 似然比检验

无约束的对数似然函数值-受约束的对数似然函数值服从卡方(q)的分布,用这个检验
$$ \ln L F_{r}=-\frac{N}{2}\ln\hat{\sigma}^{2}-\frac{N}{2}\ln(2\pi)-\frac{1}{2}\sum_{t=1}^{T}\frac{(\ln Q_{t}-\hat{\beta}_{0}-\hat{\beta}_{1}(\ln K_{t}-\ln L_{t})-\ln L_{t})^{2}}{\hat{\sigma}^{2}} $$
![[Pasted image 20240609231618.png]]
似然比检验统计量可写为
$$
LR=2\left(\ln LF_u-\ln LF_r\right),
$$
在原假设成立时，
$$
LR\overset{a}{\sim}\chi^2(q),
$$
其中 $q$ 为原假设中的约束个数。
这是一个大样本检验,因为这里应该使用西格玛而非西格玛hat,这里只能用西格玛hat,在大样本条件下西格玛hat渐进等于西格玛.

# 线性回归模型的扩展

## 含有对数变化的模型

nY, =£{,+f, nx, + BX, +6, Y, =7,+y,nX,, +Y,X>; + &, p= Auth 7 对 闷 的 弹性 , XNA ATER Xx, 每 增 AX /X; 加 一 个 百分点 ， 了 平均 增加 太 个 百分点 。 p,- aly 半 弹 性 , X NBR ARTE PR 2 每 增加 一 个 ”4 单位 ，7 平均 增加 100x 2 个 百分点 。 -和 半 弹 性 , = 不 变 的 条 件 下 ，x 每 增加 一 个 AX /X, 单位 ,7 平均 增加 001x nn 个 百分点 。
![[Pasted image 20240608163139.png]]

## 环境~~库涅滋~~ 库兹涅茨曲线

==这是一个线性的方程，但对 GDP 求导不能直接反映偏效应（需明确控制变量与函数形式）==
环境 库 效 涅 茨 曲 线 的 回归 方程 : WG, = B, + B,PGDP. + B,PGDP +, WG: HES: PGDP: 人 均 GDP C1) 回归 系数 不 再 反映 解释 变量 对 被 解释 变量 的 偏 效 应 a = 8. +28,PGDP (2) 多 项 式 模型 可 以 帮助 估计 经 济 关 系 发 生 转 换 的 位 置 经 济 关 系 发 生 转 换 的 位 置 是 : B, +2B,PGDP=0 即 PGDP =~
二次项模型的转折点条件为 $\beta_1+2\beta_2PGDP=0$。

# 多元回归分析实例:货币需求分析

## JB 正态性检验

~~检验统计量的分布和正态分布的远近~~ 用样本偏度与峰度衡量偏离正态的程度，$JB\sim\chi^2(2)$（原假设：残差正态）。
SS? (K-3) 1B = nf KI 6 24 SARE FEIN ARE; K 为 其 峰 度 。
![[Pasted image 20240608190012.png]]
JB统计量服从卡方(2)分布

# 分布滞后模型与解释变量的选择

## 分布滞后模型

模型包含解释变量的滞后项
$$ \begin{array}{l l}{{I_{t}=\gamma+\alpha_{0}P_{t}+\alpha_{1}P_{t-1}+\cdots\alpha_{m}P_{t-1}+\cdots\alpha_{m}P_{t-m}+\xi_{t}}}\\ {{\mathrm{~ijljjljigk}\mathrm{~if~i~jlj~jlj~}\pm\mathrm{~jlg~}^{\mathrm{~if~jlij~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jll~jlj~jlj~jlj~jlj~jlj~jlj~jlj~jl~jlj~jll~jlj~jll}^{\mathrm{~ifl}~j l l}~j l l l}~j l l l l}^{\mathrm{~jll}}}\\ {{\mathrm{~ifljjl}}}&{{\alpha_{0}}}&{{\alpha_{0}}}\\ {{\mathrm{~iflijljllljljlllllljlllllljllllljljllllllllll}}}}&{{\beta l l l l l l l l l l}}}&{{\frac{\beta l l l l l l l l l l l l l l l l l l l}}}\\ {{{\beta l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l l $$
![[Pasted image 20240608193612.png]]

## 滞后项阶数的选择

从最大的依次往下减.使用信息准则判断减的对不对

### 赤池信息准则AIC

$$ A I C(m)=\ln{\frac{R S S}{T}}+{\frac{2(m+2)}{T}} $$
其中T是总的观测项的个数,m是滞后项的阶数.

### 施瓦茨准则SC

$$ S C(m)=\ln{\frac{R S S}{T}}+{\frac{m+2}{T}}\ln T $$
![[Pasted image 20240608200200.png]]

调整m使得AIC或SC最小
