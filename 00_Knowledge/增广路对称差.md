---
aliases:
  - "沿增广路取对称差会使匹配增加一条边"
  - 增广路翻转
  - Augmenting-path flip
student_os: knowledge-atom
atom_id: MCS-MATCH-006
atom_type: proposition
status: source-checked
mastery_state: unassessed
requires:
  - "[[增广路定义]]"
part_of:
  - "[[二分图匹配与 Hall 定理.canvas]]"
---

# 沿增广路取对称差会使匹配增加一条边

<!-- bilingual-en:start -->
*Taking the symmetric difference along an augmenting path increases a matching by one edge*
<!-- bilingual-en:end -->

> [!summary] 原子操作
> 若 $P$ 是相对于 matching $M$ 的 augmenting path，令
> $$
> M'=M\mathbin{\triangle}E(P).
> $$
> 这等于删去路径上原属 $M$ 的边，并加入路径上原不属 $M$ 的边。所得 $M'$ 仍是 matching，而且 $|M'|=|M|+1$。
>
> <!-- bilingual-en:start -->
> If $P$ is an augmenting path relative to matching $M$, let
> $$
> M'=M\mathbin{\triangle}E(P).
> $$
> This removes the path edges formerly in $M$ and inserts those formerly outside $M$. The result $M'$ is still a matching, and $|M'|=|M|+1$.
> <!-- bilingual-en:end -->

路径的每个内部顶点原本接触一条路径内 matching edge 和一条 non-matching edge；翻转后只是把“选中哪一条”交换，因此内部顶点仍只接一条选中边。两个端点原本未匹配，翻转后各接上第一条或最后一条边，也不会制造端点冲突。
<!-- bilingual-en:start -->
Every internal vertex of the path is incident to one matched and one unmatched path edge. Flipping merely swaps which one is selected, so each internal vertex remains incident to exactly one selected edge. The two endpoints were unmatched and acquire only the first or last path edge, so no endpoint conflict is created.
<!-- bilingual-en:end -->

augmenting path 以 non-matching edge 开始和结束，因此若其中有 $k$ 条 matching edges，就有 $k+1$ 条 non-matching edges。翻转净增加一条边，这不是“路径通常更好”的直觉，而是可逐边核对的计数结论。
<!-- bilingual-en:start -->
An augmenting path begins and ends with unmatched edges. Therefore, if it contains $k$ matched edges, it contains $k+1$ unmatched edges. The flip gains exactly one edge; this is a checkable counting fact, not merely an intuition that the new path looks better.
<!-- bilingual-en:end -->

在路径 $b-1-a-2$ 中，若 $a-1$ 已选，翻转会删除 $a-1$，加入 $b-1$ 与 $a-2$。一个原有配对被替换成两个配对，matching 大小从 $1$ 变成 $2$。
<!-- bilingual-en:start -->
On the path $b-1-a-2$, if $a-1$ is selected, the flip removes $a-1$ and inserts $b-1$ and $a-2$. One old assignment is replaced by two, increasing the matching size from one to two.
<!-- bilingual-en:end -->

> [!question]- 回忆与辨别
> 为什么增广路翻转不会让内部顶点同时接触两条 matching edges？
>
> **答案：** 内部顶点在路径上恰接一条原 matching edge 与一条原 non-matching edge；翻转只交换两者的选中状态。

## 来源与核验

<!-- bilingual-en:start -->
*Sources and verification*
<!-- bilingual-en:end -->

- MIT OpenCourseWare 18.997，[Lecture 1: Non-Bipartite Matching](https://ocw.mit.edu/courses/18-997-topics-in-combinatorial-optimization-spring-2004/cb34901551f24affa0c147af1cb9151a_co_lec1.pdf)，第 1–2 页：核验对称差翻转仍为 matching，并使基数净增 $1$。
  <!-- bilingual-en:start -->
  MIT OpenCourseWare 18.997, [Lecture 1: Non-Bipartite Matching](https://ocw.mit.edu/courses/18-997-topics-in-combinatorial-optimization-spring-2004/cb34901551f24affa0c147af1cb9151a_co_lec1.pdf), pp. 1–2, verifies that symmetric-difference flipping preserves the matching property and increases cardinality by one.
  <!-- bilingual-en:end -->
