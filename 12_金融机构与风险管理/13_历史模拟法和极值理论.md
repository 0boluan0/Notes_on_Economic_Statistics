# 1. **历史模拟法（Historical Simulation Method）**

**定义与假设：** 历史模拟法是假设未来的收益率分布与过去历史分布相同 。也就是说，不对资产收益作特定分布假设，而是直接利用历史数据来模拟未来可能的情景 。具体做法是将历史上的市场因子或资产价格变动视为未来可能发生的情景集合，通过这些情景计算投资组合的损益分布，并以经验分布的分位数估计风险价值（VaR） 。==这个方法的核心假设是**历史重现**：过去发生过的市场变化模式在未来仍可能出现。==

**计算步骤：** 利用历史模拟法计算VaR的一般步骤如下：

1. **数据准备：** 收集足够长度的历史价格数据，并计算相应的历史收益率或损失序列。例如，若使用过去$N$天的数据，则得到$N$天的历史收益/损失数值。    
2. **生成情景：** 将每一个历史收益率视为未来一天的一个可能情景。假设现在的投资组合价值为$V_0$，则根据每个历史收益率情景$\Delta_i$，计算出对应的明日投资组合价值$V_i = V_0 \times (1 + \Delta_i)$，从而得到$N$种未来一天的模拟结果（或直接计算损益$\text{PL}_i = V_0 \times \Delta_i$）。
3. **计算损益分布：** 将模拟得到的$N$个投资组合明日价值与当前价值之差，形成$N$个模拟损益。对这些损益从小到大排序（损失从大到小排序）。
4. **取分位数作为VaR：** 根据所需的置信水平$q$，确定经验分布中相应的分位数位置。例如，置信度99%（$q=0.99$）的一天VaR即为损益分布中最坏的$1\%$位置对应的损失值（即按损失从大到小排序后的第$\lceil (1-q)N \rceil$大损失）。这个损失取正值即为VaR。

**示例：** 假设有500个交易日的历史数据，以计算1天99%置信水平的VaR。将过去500天每一天的收益率视为未来可能的情景，则共有500种模拟结果。将这500个结果对应的损失额从大到小排序，取排在最前面的第5大损失（约等于500×1%）作为VaR估计。例如，如果第5大单日损失对应组合价值减少了250万美元，那么99%置信的一天VaR约为250万美元。类似地，可取最坏1%的损失的平均值作为99%置信水平的预期损失（ES，亦称条件VaR）。

  

**VaR估计的精确度（置信区间）：** 由于历史模拟法基于有限历史样本，求得的分位数只是总体真实分位数的近似。Kendall和Stuart的研究给出了样本分位数估计值的标准误公式 。令$q$为置信水平，$n$为样本数量，真实分布在VaR处的概率密度为$f(x_q)$，则$q$分位数估计$\hat{x}_q$的标准误近似为：

  

$$ \text{StdErr}(\hat{x}_q) \approx \frac{\sqrt{q(1-q)}}{\sqrt{n},f(x_q)}. $$

  

利用上述标准误可以构造VaR估计的置信区间。例如，若使用$n=500$条观察值估计99%分位数VaR，假定经验分布近似正态且在VaR处的密度$f(x_{0.99})\approx0.0027$ ，计算得标准误约为167万（美元）。如果VaR点估计为2500万（美元），在95%置信水平下VaR的区间估计为 $2500 \pm 1.96\times167$ 万，即约 $2170 \sim 2830$ 万美元 。一般而言，置信水平越高，VaR估计的不确定性（标准误）越大；增大样本数量可以降低标准误，但降低幅度与$\sqrt{n}$成反比。

**方法扩展：** 针对历史模拟法的局限，实务中提出了多种改进方法，以提高VaR估计对当前风险的响应或在数据有限时提高精度：
- **对历史数据赋予权重（加权历史模拟）：** 赋予最近的数据更大权重，以反映当前市场状况 。类似于EWMA模型的思想，可设置一个衰减因子$\lambda\in(0,1)$，对距离当前$i$天前的收益赋予权重$(1-\lambda)\lambda^{i-1}$。在计算VaR时，按照损失从大到小排序累计这些权重，当累计权重达到$(1-q)$（例如1% VaR对应99%置信水平即尾部1%）时所对应的损失即为VaR 。通过调整$\lambda$并进行回溯测试，可选取使模型拟合最佳的$\lambda$值。
- **波动率更新的历史模拟：** Hull和White提出在历史模拟中结合波动率的更新调整历史情景 。基本思想是：如果当前市场的波动率水平与历史某日不同，就按波动率比例调整该日的收益变化。例如，假设当前估计的波动率是历史某日波动率估计的两倍，那么将该日实际收益变动放大两倍，作为当前情景的预期变动 。这样生成的新情景集反映了当前的波动率水平。研究表明，在波动率大幅高于历史均值时，此法给出的VaR会显著高于未调整的历史模拟结果 
- **自助法（Bootstrap）模拟：** Bootstrap方法由Efron于1979年提出，是一种通过重复抽样来推断总体分布的统计技术 。在历史模拟中，当历史数据样本较少时，可采用自助法增强VaR估计的稳定性 。基本做法是：从已有的历史收益序列（大小为$n$）中反复**有放回**抽样生成大量（例如1万组）新的模拟样本 。对于每一个模拟样本，计算其VaR，然后将所有模拟得到的VaR值从小到大排序。利用这些重复模拟的VaR分布，可以估计VaR的期望值，并给出VaR估计的置信区间 。例如，若通过1万次自助重抽样得到VaR的第2.5百分位和97.5百分位值分别为530万和890万美元，那么可以95%置信认为真实VaR落在[$530, 890$]万美元区间内 。Bootstrap法有效缓解了样本不足问题，但需要较高的计算性能支持。
    
### **历史模拟法练习题**

  

**理解题 1：** 历史模拟法计算VaR的主要原理是什么？其假设条件有哪些？

**答案：** 历史模拟法利用过去实际发生的收益/损失分布来估计未来风险。主要原理是假设未来金融市场的收益率分布与历史相同，因此可以将历史价格或收益数据当作未来可能出现的情景。该方法不需要对收益分布作参数化假定，而是直接基于经验分布计算VaR 。其核心假设包括：“历史会重演”，即过去的市场波动特征在未来仍适用；同时要求有足够的历史数据代表潜在风险。只要这些假设大致成立，按照历史数据生成未来情景并取其中相应分位数损失，就可得到组合的VaR。

  

**理解题 2：** 简述两种改进历史模拟法的方法及其原理。

**答案：** 改进历史模拟法的常用方法有：（1）**加权历史模拟法**：通过对历史数据赋予衰减权重，使近期数据对VaR计算影响更大 。这提高了VaR对当前市场变化的灵敏度，例如使用指数加权移动平均（EWMA）赋权，近期大的波动将提高VaR估计。（2）**Bootstrap自助法**：通过从现有样本反复抽样来扩大模拟样本量，获取VaR估计的分布 。其原理是在样本有限时，通过计算机重复抽取模拟，近似得到总体分布，从而可以计算VaR的平均值和置信区间，提升估计可靠性 。此外，还有**波动率调整法**等，其思路是根据当前波动率水平缩放历史收益，从而使模拟情景反映当前更高或更低的风险 。

  

**计算题 1：** 某投资组合当前价值为1000万元。过去10天的日收益率（%）分别为：[-2.0, +1.5, -0.5, +0.8, -1.0, +0.4, +1.2, -3.1, +0.6, -0.7]。请使用历史模拟法估计该投资组合1天90%置信水平的VaR值。

**答案：** 90%置信水平对应1天VaR的损失分位为最坏的10%情景（因为损失超出VaR的概率为10%）。首先计算每个收益率对应的损益金额：例如收益率-2.0%对应损失$1000 \times 2.0\% = 20$万元，+1.5%对应收益15万元（损失为-15万），以此类推得到10个损益值（万元）：[-20, +15, -5, +8, -10, +4, +12, -31, +6, -7]。将这些损益从损失最大的排序：-31, -20, -10, -7, -5, +4, +6, +8, +12, +15（万元）。其中损失最大的10%对应排序最前面的第1个值，因为10个情景中最坏的1个占10%。因此VaR取该序列中的最大损失，即31万元。由于这里计算的是损失的正值，得到**VaR = 31万元**（即在90%置信水平下，一天内可能损失31万元）。如果需要，亦可通过线性插值提高精度；但在本例中样本较小且正好10%刻度，直接取最大损失即可。

  

**计算题 2：** 某投资组合用500天历史数据估计了99%置信的一天VaR为2500万元。假设在VaR处对应的损失分布密度$f(x)=0.0027$，试利用分位数标准误公式计算该VaR估计值的95%置信区间。

**答案：** 使用分位数估计的标准误公式$\displaystyle \text{StdErr}(\hat{x}_{0.99}) \approx \frac{\sqrt{0.99\times0.01}}{\sqrt{500}\times0.0027}$计算标准误。首先，$q(1-q)=0.99\times0.01=0.0099$，$\sqrt{0.0099}=0.0995$。$\sqrt{n}=\sqrt{500}\approx22.36$。因此标准误$\approx \frac{0.0995}{22.36\times0.0027} \approx 16.7$（万元）。为取得稳健结果，上式常近似使用正态95%置信区间，即$\hat{x}\pm1.96,\text{StdErr}$。将$\hat{x}_{0.99}=2500$万元代入，95%置信区间为：$2500 \pm 1.96\times16.7$万元，即约 **$2170 \sim 2830$ 万元**。这表示以95%信心，真实的99%VaR落在2.17亿元至2.83亿元之间。

  

## **极值理论（Extreme Value Theory, EVT）**

  

**基本原理：** 极值理论主要研究分布尾部的行为特征，关注极端损失发生的概率及幅度。针对金融风险管理，极值理论通常用于分析损失分布的**尾部**（高损失一端）的形状。方法上有两类：块极值法（研究一定区间内的最大值）和超阈值法（Peaks-Over-Threshold, POT）。这里侧重后者，即选定一个高阈值$u$，专注于**超出阈值的极端损失**部分。Gnedenko于1943年的定理奠定了该方法的理论基础：对于大量常见分布，当阈值$u$逐渐增大时，超出$u$的尾部数据的分布将趋近于**广义Pareto分布（GPD）** 。换言之，足够高的损失超越一定阈值后，其超额分布$F_u(y)=P(X-u \le y \mid X>u)$可以用GPD来近似建模 。这一结论不要求整体分布属于某个特定族，只关注尾部渐进性质。

  

**广义Pareto分布（GPD）：** GPD是极值理论建模尾部的核心分布族。其累积分布函数一般表示为：

- 当$\xi \neq 0$时：
    
    $$ F_{\text{GPD}}(y) = 1 - \Big(1 + \frac{\xi y}{\beta}\Big)^{-1/\xi}, \qquad (y \ge 0),$$
    
- 当$\xi \to 0$时（作为极限情况）：
    
    $$ F_{\text{GPD}}(y) = 1 - \exp!\Big(-\frac{y}{\beta}\Big), \qquad (y \ge 0).$$
    

  

其中$\beta>0$为尺度参数，$\xi$为形状参数。**形状参数$\xi$**决定了尾部的形状和厚度：$\xi>0$对应重尾分布（尾部概率以幂律衰减），例如金融数据常呈现的胖尾现象；$\xi=0$对应尾部呈指数衰减（类似指数分布，尾部适中）；$\xi<0$则尾部有界（存在损失的上限）。**尺度参数$\beta$**控制了分布的伸展（类似于标准差的作用，但针对尾部）。需要注意GPD通常用于$x>u$的部分，因此其定义域取决于所选阈值$u$及$\xi$（若$\xi<0$则$y$有最大值$-,\beta/\xi$）。根据极值理论，很多常见分布（如正态、对数正态、$t$分布等）的高端尾部，都可以在一定程度上用某个$\xi,\beta$的GPD来逼近 。

  

**参数估计（最大似然法）：** 给定选定的阈值$u$，将所有超过$u$的观测损失值记为${v_i: i=1,\dots,n_u}$，其中$n_u$是超过阈值的样本数量。极值理论采用**峰值超阈值**样本${y_i = v_i - u}$来拟合GPD模型。参数估计常用**最大似然估计（MLE）**：首先写出超额$y_i$的GPD密度函数 $f(y) = \frac{1}{\beta}\Big(1+\frac{\xi y}{\beta}\Big)^{-1/\xi - 1}$，则样本的对数似然函数为：

  

$$ \ell(\xi,\beta) = \sum_{i=1}^{n_u}\ln f(y_i) = -n_u\ln \beta -\Big(1+\frac{1}{\xi}\Big)\sum_{i=1}^{n_u}\ln\Big(1+\frac{\xi y_i}{\beta}\Big). $$

  

通过对$\ell(\xi,\beta)$分别对$\xi$和$\beta$求偏导并设为0，可以数值求解得到$\hat{\xi}$和$\hat{\beta}$ 。由于对数似然方程通常没有解析解，需借助迭代算法获得参数估计值。此外，也可以采用**PWM（概率加权矩法）**等其他方法估计参数，但MLE在大样本下较有效。实践中，阈值$u$的选择会影响参数估计和模型拟合效果。经验法则是选取足够高的$u$（通常可取经验分布95%或更高的分位数作为$u$ ），以确保超额样本集中在极端尾部；同时$u$又不能过高以至于有效数据过少。可以通过绘制尾部参数稳定图等方式来辅助选择合适的$u$值。

**VaR与ES的估计应用：** 一旦利用超阈值数据拟合出GPD模型，就可以用其推断高置信水平的VaR和ES。假设总体样本量为$n$，超过阈值$u$的样本量为$n_u$，则超过阈值的概率$\hat{p}=P(X>u)\approx n_u/n$ 。对于给定高置信水平$q$（如0.99），若$q$对应的VaR高于阈值$u$，则有：

- **VaR计算：** VaR定义为分位数，使得$P(X \le \text{VaR}_q) = q$，等价于$P(X > \text{VaR}_q) = 1-q$。当$\text{VaR}_q > u$时，超出$u$的部分服从GPD模型，满足_
    
    $$ P(X > x \mid X>u) \approx 1 - F_{\text{GPD}}(x-u) = \Big(1+\frac{\xi (x-u)}{\beta}\Big)^{-1/\xi}, \qquad x \ge u. $$
    
    则无条件的尾部超越概率为
    
    $$ P(X > x) = P(X>u),P(X > x \mid X>u) \approx \hat{p}\Big(1+\frac{\xi (x-u)}{\beta}\Big)^{-1/\xi}. $$
    
    令$x = \text{VaR}_q$并使$P(X > x) = 1-q$，解得：
    
    $$ \text{VaR}_q = u + \frac{\beta}{\xi}\Big[\Big(\frac{1-q}{\hat{p}}\Big)^{-\xi} - 1\Big], $$
    
    当$\xi \to 0$时，上式可取极限形式$\text{VaR}_q = u - \beta \ln\frac{1-q}{\hat{p}}$。通过该公式，可以利用估计的$\hat{\xi}, \hat{\beta}$外推计算远高于历史阈值的极端分位数 。
    
- **ES计算：** ES（期望损失，也称CVaR）表示在损失超过VaR水平条件下的平均损失，即$ES_q = E[X \mid X > \text{VaR}_q]$。对于$\xi < 1$的情形，GPD模型下的ES有解析形式：
    
    $$ ES_q = \frac{\text{VaR}_q}{1-\xi} + \frac{\beta - \xi,u}{1-\xi}, $$
    
    其中$\text{VaR}_q$由上述公式求得。若$\xi = 0$，则$ES_q = \text{VaR}_q + \beta$，因为此时尾部分布相当于指数分布的记忆性。上述ES公式可用于计算超出VaR之后的平均损失规模，通常$ES_q > \text{VaR}_q$，且$\xi$越大（尾部越肥厚），二者差距越明显。
    

  

_注：_ 在实际应用极值理论估计VaR/ES时，需要注意模型假设和参数稳定性。如果估计得到形状参数$\hat{\xi}\le 0$（可能表示尾部不肥厚或样本不足），需重新审视阈值选择或数据特征 。另外，应进行模型诊断（如QQ图、Kupiec跨越检验等）确保GPD拟合尾部的有效性。

  

### **极值理论练习题**

  

**理解题 1：** Gnedenko定理在极值理论中描述了什么现象？它对于金融风险管理有何意义？

**答案：** Gnedenko定理是极值理论的核心结果之一，描述了分布尾部的极限行为。简单来说，该定理指出当选取足够高的阈值$u$时，原始分布右尾部分超过$u$的**超额分布**将趋向于一个**广义Pareto分布（GPD）** 。这一现象与具体的原始分布类型无关，具有普适性。因此，对于金融风险管理而言，Gnedenko定理的意义在于提供了**建模极端风险的通用方法**：不论基础损失分布是什么，只要我们聚焦极大损失的尾部并选取高阈值，就可以使用GPD近似建模。这为计算罕见但冲击巨大的风险指标（如高置信水平VaR和ES）奠定了理论基础，使我们能够用统一的方法外推估计超出历史经验范围的极端损失概率。

  

**理解题 2：** 广义Pareto分布的形状参数$\xi$和尺度参数$\beta$分别反映了什么含义？在风险度量中形状参数有什么作用？

**答案：** GPD的形状参数$\xi$和尺度参数$\beta$共同决定了尾部分布的特征。其中**形状参数$\xi$直接影响尾部厚度：$\xi>0$表示重尾，尾部概率衰减缓慢，极端损失更频繁（例如$\xi=0.2$可能意味着分布尾比正态更肥尾)；$\xi=0$表示尾部呈指数衰减，极端损失概率以中等速度递减；$\xi<0$则尾部截断，存在损失的上限。形状参数在风险度量中至关重要，它决定了极端置信区间下VaR随置信水平的增长速度**——$\xi$越大，高分位数VaR随置信水平提升而增长越快，因为尾巴更肥厚。**尺度参数$\beta$**则类似于尾部的放大系数，控制尾部的基本波动幅度，$\beta$越大，在相同$\xi$下尾部越分散，VaR数值也越高。总结而言，$\xi$刻画尾部形状类型，$\beta$设定尾部尺度大小。在极值模型中准确估计这两个参数，可以更可靠地评估金融资产组合面临的极端损失风险。

  

**计算题 1：** 某投资组合基于1000个交易日的损失数据应用POT方法进行极值分析。选定损失阈值$u=100$万元，共有$n_u=50$个损失超过该阈值。利用这些超额损失拟合得到GPD参数估计值$\hat{\xi}=0.20$，$\hat{\beta}=10$（万元）。试根据极值理论计算该组合99%置信水平的一天VaR。

**答案：** 首先计算超过阈值的经验尾部概率$\hat{p}=n_u/n=50/1000=0.05$，表示损失超过100万元的概率约为5%。99%置信水平对应损失分布的0.99分位，即超尾部概率$1-q=0.01$。由于$0.01<0.05=\hat{p}$，99% VaR落在阈值$u$以上，可用GPD模型外推计算。公式为：

  

$$ \text{VaR}_{0.99} = u + \frac{\hat{\beta}}{\hat{\xi}}\Big[\Big(\frac{1-q}{\hat{p}}\Big)^{-\hat{\xi}} - 1\Big]. $$

将数值代入：$\frac{1-q}{\hat{p}} = \frac{0.01}{0.05} = 0.2$。于是$\big(\frac{1-q}{\hat{p}}\big)^{-\hat{\xi}} = 0.2^{-0.20} = 0.2^{-0.2}$. 计算$0.2^{-0.2} = \exp(-0.2\ln0.2) \approx \exp(0.3218) \approx 1.379$（可使用计算器完成精确计算）。因此，

$$ \text{VaR}_{0.99} \approx 100 + \frac{10}{0.20},(1.379 - 1) = 100 + 50 \times 0.379 = 100 + 18.95 = 118.95~(\text{万元}). $$

约为119万元。**答：** 99%置信水平下一天VaR约为 **119万元**。这意味着有1%的概率，一天内损失将超过119万元。

**计算题 2：** 在上述极值模型中，进一步求99%置信水平下的预期损失（ES）。

**答案：** 可直接利用GPD给出的ES公式：

  

$$ ES_{0.99} = \frac{\text{VaR}_{0.99}}{,1-\hat{\xi},} + \frac{\hat{\beta} - \hat{\xi},u}{,1-\hat{\xi},}. $$

  

将$\text{VaR}_{0.99}\approx118.95$、$\hat{\xi}=0.20$、$\hat{\beta}=10$、$u=100$代入：首先$1-\hat{\xi}=0.80$。计算$\hat{\beta}-\hat{\xi}u = 10 - 0.2\times100 = 10 - 20 = -10$。因此：

  

$$ ES_{0.99} = \frac{118.95}{0.80} + \frac{-10}{0.80} = \frac{118.95 - 10}{0.80} = \frac{108.95}{0.80} = 136.19~(\text{万元}). $$

  

结果约为136.2万元。**答：** 99%置信水平的预期损失约为 **136万元**。这表示如果实际损失超过VaR水平（约119万），平均损失将达到136万左右。

# 作业

## 13.3



## 13.8

## 13.12

## 13.17

