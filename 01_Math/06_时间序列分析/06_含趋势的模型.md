# 0.回忆用

# 1. 引言

单位根存在于生活中

不平稳就不能用Box-Jenkins 三步法

# 2. 确定趋势模型

可以先假定一个趋势模型,然后使用估计后的残差进行Box-Jenkins建模
 
# 3. 随趋势([[Unit Root Test|单位根]])模型

## 3.1 定义

>[!note] 单位根过程(差分平稳)
>Nelson 和 Plosser (1982) 的研究表明，许多重要的宏观经济变量更可能是**单位根过程**而非**趋势平稳过程**。换言之，这些序列经过一次差分后才会平稳（即差分平稳），而不是通过简单去除一个确定性趋势就能平稳。

## 3.2 举例

>[!example] 随机游走模型(特殊的单位根过程)
>
假设
> 
> $$y_t = y_{t-1} + \varepsilon_t,$$
> 
> 其中 $\varepsilon_t$ 为均值为 0、方差为 $\sigma^2$ 的独立同分布随机误差项（${\varepsilon_t}$ 独立同分布且相互独立）。这种模型称为随机游走 ([[Random Walk|random walk]])，它是单位根随机趋势模型的一个特殊情形。
> 
> 如果给定初始值 $y_0$，那么该随机游走过程的解为：
> 
> $$y_t = y_0 + \sum_{i=1}^t \varepsilon_i.$$
> 
> 根据这一解，我们可以得到：$\mathbb{E}[y_t] = y_0$，即随机游走在无漂移项的情况下，其期望保持为初始值（不随时间变化）。

>[!example] **带漂移的随机游走**模型形式为：
> $$ y_t = y_{t-1} + a_0 + \varepsilon_t. $$
> 给定初始值 $y_0$，我们得到：
> $$ y_t = y_0 + a_0t + \sum_{i=1}^{t} \varepsilon_i. $$
> 可以看出，该过程包含两个非平稳成分：一个线性的确定性趋势 $a_0 t$ 和一个随机趋势 $\sum_{i=1}^{t} \varepsilon_i$。

## 3.3 差分

对于存在单位根的过程，我们可以通过取差分使其变为平稳过程。以随机游走加漂移模型为例：
$$ \[[Delta|Delta]] y_t = a_0 + \varepsilon_t. $$
很容易看出，$\[[Delta|Delta]] y_t$ 是平稳的过程。

对于一般的 [[ARIMA|ARIMA]](p, 1, q) 模型：
$$ A(L)y_t = B(L)\varepsilon_t, $$
如果 $A(L)$ 有一个单位根且 $B(L)$ 的所有根都在单位圆之外，那么：
$$ (1 - L)A^*(L)y_t = B(L)\varepsilon_t. $$
这意味着：
$$ A^*(L)\[[Delta|Delta]] y_t = B(L)\varepsilon_t $$

 且由于 $A^*(L)$ 的所有根都在单位圆之外，推得 $\[[Delta|Delta]] y_t$ 是平稳过程。

 **注：** 可以将上述结论推广到一般的 [[ARIMA|ARIMA]](p, d, q) 模型。总体而言，==如果一个序列有 $d$ 个单位根，那么经过 $d$ 次差分后该序列将成为平稳序列。这类过程称为积分阶为 $d$ 的过程，记作 $I(d)$。==其中单位根过程就是积分阶为1的过程，即 $I(1)$ 过程。

# 4.[[Unit Root Test|单位根检验]]

参见：[[单位根检验]] · [[平稳性检验对比(DF-ADF-KPSS)]]

由于假设不平稳,大样本性质和中心极限定理不能用,所以有新的假设.

## 4.1 DF检验

**目的**：检验序列 $y_t$ 是否存在单位根（即非平稳）。

**基本模型**

假设
$$
y_t = \phi y_{t-1} + \varepsilon_t,
$$
其中 $\varepsilon_t$ 为白噪声。令 $\delta = \phi - 1$，则等价于
$$
\[[Delta|Delta]] y_t = y_t - y_{t-1} = \delta y_{t-1} + \varepsilon_t.
$$

原假设 $H_0: \phi = 1$（即 $\delta = 0$，存在单位根），备择假设 $H_1: \phi < 1$（平稳）。

**检验统计量**
基于OLS 估计 $\delta$，计算 $t$ 统计量：

$$
\tau = \frac{\hat\delta}{\mathrm{SE}(\hat\delta)}.
$$

与临界值比较（Dickey–Fuller 表）来决定是否拒绝 $H_0$。


## 4.2 [[Augmented Dickey-Fuller Test|ADF检验]]

**目的**：在 DF 检验基础上，考虑残差序列可能存在自相关，通过增加滞后差分项来修正。

**基本模型**

带截距和趋势的形式为

$$
\[[Delta|Delta]] y_t = \alpha + \beta t + \delta y_{t-1}
- \sum_{i=1}^p \theta_i\[[Delta|Delta]] y_{t-i} + \varepsilon_t  $$

- $\alpha$ 为常数项，
- $\beta,t$ 为趋势项（可选），
- $p$ 为滞后阶数，用于消除自相关。

**检验假设**

$$
H_0: \delta = 0\quad(\text{存在单位根}),\qquad
H_1: \delta < 0\quad(\text{平稳}).
$$

使用的检验统计量和DF一样,但是临界条件不一样.

## 4.3 KPSS检验

**目的**：与 DF/[[Augmented Dickey-Fuller Test|ADF]] 相反，KPSS 检验的原假设是序列平稳，备择假设是存在单位根。

**基本模型**

将 $y_t$ 分解为趋势和误差：
$$

y_t = \mu_t + \varepsilon_t,

\qquad

\mu_t = \mu_{t-1} + \eta_t,

$$

其中 $\varepsilon_t$ 和 $\eta_t$ 均为白噪声。若 $\eta_t$ 方差为 0，则 $\mu_t$ 为常数，$y_t$ 平稳。

**检验统计量**

先计算残差序列 $\hat\varepsilon_t$ 并构造累计和
$$
S_t = \sum_{i=1}^t \hat\varepsilon_i.
$$
然后计算
$$
\mathrm{LM} = \frac{1}{T^2}\sum_{t=1}^T S_t^2 \bigg/ \hat\sigma^2,
$$
其中 $\hat\sigma^2$ 为 $\varepsilon_t$ 的长期方差估计。与 KPSS 临界值比较：
- 若 $\mathrm{LM}$ 大于临界值，拒绝 $H_0$（即非平稳）；
- 否则不拒绝，认为序列平稳。

## 4.4 三检验比较

|**特点**|**DF/[[Augmented Dickey-Fuller Test|ADF]] 检验**|**KPSS 检验**|
|---|---|---|
|原假设|存在单位根（非平稳）|平稳|
|备择假设|平稳|存在单位根（非平稳）|
|适用情况|检测是否含单位根|验证序列平稳性|
|对趋势/截距的处理|可选截距、趋势|通常分为 “水平平稳” 和 “含趋势平稳” 两种情况|

# 5. 蒙特卡洛方法

用于通过数值模拟评估统计量/检验在有限样本下的性质（如拒真率/功效），本节不作展开。~~无意义~~

# 作业

## 2. 
2. 给定初始条件 $y_0$，求出下面不同的带无规则成分的趋势模型的通解和预测函数（即 $E_t y_{t+s}$）：

a. $y_t=\mu_t+v_t$，式中
$$
\mu_t=\mu_{t-1}+\varepsilon_t,\quad v_t=(1+\beta_1L)\eta_t,\quad E(\varepsilon_t\eta_t)=0.
$$

b. $y_t=\mu_t+v_t$，式中
$$
\mu_t=\mu_{t-1}+\varepsilon_t,\quad v_t=(1+\beta_1L)\eta_t,
$$
并且 $\varepsilon_t$ 与 $\eta_t$ 的相关系数为 1。

c. 求每个模型的 [[ARIMA|ARIMA]] 表达式。

## 3.
3. 如本书所述，有一个单位根的序列，其 [[Autocorrelation Function|ACF]] 说明衰退的可能性不大；但是，在移动平均项为负的序列中可能很难发现单位根。考虑单位根过程
$$
y_t=y_{t-1}+\varepsilon_t-0.8\varepsilon_{t-1}.
$$

a. 根据 $\{\varepsilon_t\}$ 和初始条件 $y_0$ 逆推 $y_t$。  
b. 根据 $\{\varepsilon_t\}$ 和初始条件 $y_0$，对 $y_t$ 使用待定系数法。  
（提示：解的形式为 $y_t=\sum_{i=0}^{t-1}\alpha_i\varepsilon_{t-i}+y_0$）  
c. 用 a、b 的答案得出 [[Autocorrelation Function|ACF]] 的前几项。  
d. 解释 MA 的负数项如何影响 [[Autocorrelation Function|ACF]] 的形状；特别地，解释序列如何“无限持续”，即使 [[Autocorrelation Function|ACF]] 系数远小于 1。
